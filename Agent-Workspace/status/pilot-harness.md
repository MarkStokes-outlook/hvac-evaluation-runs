# Pilot harness preparation — 2026-09-15

Ready to administer the labelled FrostLine V1 pilot. All changes are under
`hvac-evaluation-runs`; both source repositories remain clean and frozen refs unchanged.

- Tagged candidate extracted, dependencies copied, SPA built, clean schema-based
  baselines and exact allowlisted initial inputs prepared for all 27 scenarios.
- Native fixture projections are conservative; every fact/unknown remains verbatim
  in visible source packets and native inbox entries. Adaptations/defaults logged.
- macOS candidate confinement, recorded supported API transport, operator API
  runner, read-only SQLite/upload snapshots, browser screenshots/DOM/network
  capture, append-only hash-chained traces and immutable evidence manifests prepared.
- Declared stage delivery verified with synthetic administrator state, preserving
  database/history. No real scenario task executed and no candidate score created.
- 478 readiness checks plus 4 administration tests passed. Smoke processes stopped.
- No external credential required for the operational operator profile; AI disabled.
- Full runbook: `pilot-001/README.md`. Readiness evidence: `pilot-001/readiness/`.
- Next action: start B001 with a fresh attempt, acknowledge fidelity/context, give
  the exact public task in a separate operator context, operate through recorded
  API/UI, capture every stage and seal evidence. Scoring remains a later step.
- Global `~/Source/AGENTS.md` was missing; available project instructions and
  explicit user scope governed this preparation.

Annotated tag objects differ from commit IDs; both frozen tags resolve to current
HEAD commits. Extraction used tags throughout; provenance distinguishes both IDs.
